<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.ama.luojl.jobschedulerdemoapp.MainActivity">

    <TextView
        android:id="@+id/infoView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="10"
        android:scrollbars="vertical"
        android:text="Hello"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="18dp" />

    <Button
        android:id="@+id/startJobBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="50dp"
        android:text="Start"
        android:layout_marginStart="39dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true" />

    <Button
        android:id="@+id/cancelJobBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cancel"
        android:layout_alignBaseline="@+id/startJobBtn"
        android:layout_alignBottom="@+id/startJobBtn"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/cancelAllJobBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cancel All"
        android:layout_alignBaseline="@+id/cancelJobBtn"
        android:layout_alignBottom="@+id/cancelJobBtn"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="39dp" />

</RelativeLayout>
